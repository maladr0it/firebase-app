const defaultState = {
  id: '3isyyZt5AZEWpXDVRokV' // name: zami
};

const user = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default user;